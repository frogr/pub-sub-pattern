<%= form_for Event.new do |f| %>
  <%= f.text_field :event_name %>
  <%= f.submit "Send event" %>
<% end %>

<% Event.all.each do |event| %>
  <%= event.name %>
  <%= event.timestamp %>
  <br>
<% end %>

<h2>Alert Status</h2>

<% if @alert.status == "all_clear" %>
  <div class="all-clear">
    All clear! 👍
  </div>
<% elsif @alert.status == "yellow_alert" %>
  <div class="yellow">
    Yellow alert! ⚠️
  </div>
<% else %>
  <div class="red">
    Red alert! 🚨
  </div>
<% end %>