<%= form_for Event.new do |f| %>
  <%= f.text_field :event_name %>
  <%= f.submit "Send event" %>
<% end %>

<% Event.all.each do |event| %>
  <%= event.name %>
  <%= event.timestamp %>
  <br>
<% end %>